name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: 
          echo "Hello World!"
          echo "Current Time is $(date)"
          echo "Current Date is $(date +'%Y-%m-%d')"
          echo "Done!"
