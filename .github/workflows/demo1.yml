name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: 
          echo "Test Run!!!"
          echo "commit id: $GITHUB_SHA"
          echo "repo: $GITHUB_REPOSITORY"
